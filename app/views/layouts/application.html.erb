<!DOCTYPE html>
<html>
<head>
  <title>TestGuru</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
<% if logged_in? %>
  <p class="nav user">Добро пожаловать, Гуру <b><%= current_user.name %></b></p>
  <%= button_to 'Выйти из системы', logout_path, method: :delete %>
<% else %>
  <%= link_to 'Регистрация', signup_path %>
  <%= link_to 'Вход', login_path %>
<% end %>

<h1><%= link_to 'TestGuru', tests_path %></h1>

<%= yield %>

<%= render 'layouts/footer' %>
</body>
</html>
